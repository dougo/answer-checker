source 'https://rubygems.org'

gem 'rails'

# App server:
gem 'puma'

# No longer bundled with Ruby 3, but Rails needs it:
gem 'rexml'

group :test do
  # System testing support:
  gem 'capybara'
  gem 'webdrivers'
end
